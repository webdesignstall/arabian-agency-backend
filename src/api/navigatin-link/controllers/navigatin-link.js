'use strict';

/**
 * navigatin-link controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::navigatin-link.navigatin-link');
